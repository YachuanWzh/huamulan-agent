# Resolve Time

Use this skill when the user asks about the current time, dates, relative dates,
timezone conversion, or scheduling language that depends on "today", "tomorrow",
or similar expressions.

Available tools:

- `resolve_current_time`: returns the current date and time for an IANA timezone.
